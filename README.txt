This repository stores jira attachments
(binary files) that have been migrated and
linked from gh issues.

All attachments are on the 'jira' branch, which is not connected 
to this branch (so that they're not downloaded when somebody does
a simple clone).